{
  "compilerOptions": {
    // Куда производится сборка
    "outDir": "./dist/",
    // Подсвечивает места, где не указан тип
    // Это не значит, что теперь вовсе нельзя исп-ть any
    // Это означает, что в подобных сит-х разраб должен явно писать any
    "noImplicitAny": true,
    "module": "es6",
    // Указывает версию, в которую код будет компилиться для большинства браузеров
    "target": "es5",
    "jsx": "react-jsx",
    // Компилятор будет обрабатывать не только TS, но и JS файлы
    "allowJs": true,
    "moduleResolution": "node",
    // esModuleInterop - позволяет работать с пакетами, которые исп-т
    // common js как с обычными пакетами с помощью import require() .module.export = common js
    // Например: import Moment from 'moment';
    // console.log(Moment) - вернет undefined
    // с флагом результат [object Object]
    "esModuleInterop" : true,
    // Если какая-нть библиотека не имеет default import,
    // лоадеры вроде ts-loader или babel-loader автоматически создают их
    // Вместо такого импорта - import * as React from 'react';
    // можно написать import React from 'react'
    "allowSyntheticDefaultImports" : true,
    // Для абсолютного импорта
    "baseUrl": "."
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}